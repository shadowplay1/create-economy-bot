import { CommandArgumentsParser } from './utils/CommandArgumentsParser.util'

const commandArgumentsParser = new CommandArgumentsParser()
commandArgumentsParser.parse()

// throw new Error('The package is not ready yet. Come back later when it\'s ready!')

